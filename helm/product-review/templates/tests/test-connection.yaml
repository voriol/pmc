apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-review.fullname" . }}-test-connection"
  labels:
    {{- include "product-review.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-review.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
